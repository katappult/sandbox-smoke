# CLAUDE.md

This project is generated by Katappult Low Code Platform.
Everything under `**/generated/**`, `_generated`, or with a name including `generated` is **auto-generated** and must not be edited — it will be overwritten by the generator.

**Stack:** Java 21 + Spring Boot 4 backend (`src/`) · Next.js 15 + React 18 frontend (`frontend/`)

---

- Don't modify generated code under `**/generated/` or with name including `generated`.
- Don't add new files under a generated folder.
- Don't name a variable `s`, `d`, or any non-descriptive name except for loop indices.
- Don't mix backend and frontend changes in the same task. If a feature requires both, split it into two separate tasks.
- Backend tasks must be implemented before frontend tasks, to avoid blocking frontend development.
- After backend modification always run all tests.
